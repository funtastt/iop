# Чеклист для сдачи Задания 1

## Что нужно отправить преподавателю:

### 1. Ссылка на GitHub репозиторий
- URL: [вставьте ссылку на репозиторий]
- Ветка: master
- Убедитесь, что код закоммичен и запушен

### 2. Архив с выкачанными страницами
Команда для создания архива:
```bash
# Windows (PowerShell):
Compress-Archive -Path pages -DestinationPath pages.zip

# Linux/macOS:
zip -r pages.zip pages/
```
Файл: `pages.zip`

### 3. Файл index.txt
Файл: `index.txt` (находится в корне проекта)

---

## Проверка перед отправкой:

- [ ] Скачано минимум 100 страниц
- [ ] Все файлы сохранены с HTML-разметкой
- [ ] Файл `index.txt` создан и соответствует формату
- [ ] `README.md` содержит инструкции по запуску
- [ ] Код содержит комментарии на русском языке
- [ ] Код запущен и протестирован
- [ ] Все зависимости указаны в `requirements.txt`
- [ ] Репозиторий на GitHub содержит актуальный код

## Как получить материалы для отправки:

1. **Ссылка на GitHub**:
   - Скопируйте URL вашего репозитория

2. **Архив страниц**:
   - Windows: `Compress-Archive -Path pages -DestinationPath pages.zip`
   - Linux: `zip -r pages.zip pages/`

3. **index.txt**:
   - Файл создаётся автоматически при запуске `crawler.py`
   - Находится в корне проекта
